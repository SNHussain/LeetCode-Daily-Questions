class Solution {
public:
    int findClosest(int x, int y, int z) {
        int Person1= abs(z-x);
        int Person2= abs(z-y);

        if(Person1 < Person2){
            return 1;
        }
        else if(Person2 < Person1){
            return 2;
        }

        return 0;
    }
};
